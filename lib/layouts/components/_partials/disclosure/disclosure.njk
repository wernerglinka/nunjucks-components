{#
  disclosure
  Renders a list of collapsed disclosure blocks using the native
  details/summary elements: accessible, keyboard-operable, and
  JS-free. Body content is markdown.

  Named for the spec term: details/summary form a "disclosure widget".
  The name "accordion" is taken by the accordion section, which covers
  the JS-driven, one-open-at-a-time interaction; this partial is the
  lightweight primitive for collapsed boilerplate inside other
  sections.

  Parameters:
  - items: array of { title, prose }
#}

{% macro disclosures(items) %}
  <div class="disclosures">
    {% for item in items %}
      <details class="disclosure">
        <summary>{{ item.title }}</summary>
        <div class="disclosure-body prose flow">{{ item.prose | mdToHTML | safe }}</div>
      </details>
    {% endfor %}
  </div>
{% endmacro %}
